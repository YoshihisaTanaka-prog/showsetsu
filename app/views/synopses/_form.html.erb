<%= form_with(model: synopsis) do |form| %>
  <% if synopsis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(synopsis.errors.count, "error") %> prohibited this synopsis from being saved:</h2>

      <ul>
        <% synopsis.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
